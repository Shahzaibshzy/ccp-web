{
  "name": "inventory-management-system",
  "version": "1.0.0",
  "private": true,
  "description": "Inventory management app with React, Express, and SQLite",
  "scripts": {
    "build": "npm --prefix frontend ci && npm --prefix frontend run build",
    "start": "node backend/server.js"
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "sqlite3": "^5.1.6"
  },
  "engines": {
    "node": ">=18"
  }
}
